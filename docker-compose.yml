services:
  dev:
    build: .
    volumes:
      - .:/workspace
      - build-cache:/opt/micropython/ports/esp32/build-ESP32_GENERIC_S3
    stdin_open: true
    tty: true
    command: sleep infinity

volumes:
  build-cache:
